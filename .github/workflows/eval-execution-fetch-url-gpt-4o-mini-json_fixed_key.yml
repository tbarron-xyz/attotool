name: Eval Execution Fetch URL - openai/gpt-4o-mini - json_fixed_key

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-fetch-format-reusable.yml
    with:
      short_model: openai/gpt-4o-mini
      full_model: openai/gpt-4o-mini
      format: json_fixed_key
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}