name: Eval Read 8 Files Read - openai-gpt-4o-mini - json

on:
  workflow_run:
    workflows: ["Eval Execution Read - openai-gpt-4o-mini - json"]
    types:
      - completed

jobs:
  evaluate:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/eval-read-8-files-format-reusable.yml
    with:
      model: openai-gpt-4o-mini
      run_id: ${{ github.event.workflow_run.id }}
      format: json