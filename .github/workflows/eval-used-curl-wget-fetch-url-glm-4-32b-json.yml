name: Eval Used CURL or WGET Fetch URL - glm-4-32b - json

on:
  workflow_run:
    workflows: ["Eval Execution Fetch URL - glm-4-32b - json"]
    types:
      - completed

jobs:
  evaluate:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/eval-used-curl-wget-format-reusable.yml
    with:
      model: glm-4-32b
      run_id: ${{ github.event.workflow_run.id }}
      format: json
