name: Eval Execution Fetch URL - mistral-small-3.1-24b - json

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-fetch-format-reusable.yml
    with:
      short_model: mistral-small-3.1-24b
      full_model: mistralai/mistral-small-3.1-24b-instruct
      format: json
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}