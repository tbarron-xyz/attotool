name: Eval Execution Fetch URL - gpt-oss-120b-exacto - json

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-fetch-format-reusable.yml
    with:
      short_model: gpt-oss-120b-exacto
      full_model: openai/gpt-oss-120b:exacto
      format: json
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}