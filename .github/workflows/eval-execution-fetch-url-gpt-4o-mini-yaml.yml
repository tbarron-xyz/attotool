name: Eval Execution Fetch URL - openai/gpt-oss-20b - yaml

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-fetch-format-reusable.yml
    with:
      short_model: openai/gpt-oss-20b
      full_model: openai/gpt-oss-20b
      format: yaml
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}