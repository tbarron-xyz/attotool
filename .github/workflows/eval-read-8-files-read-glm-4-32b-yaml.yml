name: Eval Read 8 Files Read - glm-4-32b - yaml

on:
  workflow_run:
    workflows: ["Eval Execution Read - glm-4-32b - yaml"]
    types:
      - completed

jobs:
  evaluate:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/eval-read-8-files-format-reusable.yml
    with:
      model: glm-4-32b
      run_id: ${{ github.event.workflow_run.id }}
      format: yaml
