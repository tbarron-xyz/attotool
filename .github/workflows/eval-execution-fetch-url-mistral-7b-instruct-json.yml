name: Eval Execution Fetch URL - mistral-7b-instruct - json

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-fetch-format-reusable.yml
    with:
      short_model: mistral-7b-instruct
      full_model: mistralai/mistral-7b-instruct
      format: json
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}