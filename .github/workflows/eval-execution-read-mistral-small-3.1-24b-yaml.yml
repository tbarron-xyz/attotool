name: Eval Execution Read - mistral-small-3.1-24b - yaml

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-read-format-reusable.yml
    with:
      short_model: mistral-small-3.1-24b
      full_model: mistralai/mistral-small-3.1-24b-instruct
      format: yaml
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}