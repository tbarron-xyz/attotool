name: Eval Execution Read - google/gemini-2.5-flash-lite - json_fixed_key

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-read-format-reusable.yml
    with:
      short_model: google-gemini-2.5-flash-lite
      full_model: google/gemini-2.5-flash-lite
      format: json_fixed_key
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}