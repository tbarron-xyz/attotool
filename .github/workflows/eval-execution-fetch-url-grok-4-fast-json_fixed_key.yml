name: Eval Execution Fetch URL - grok-4-fast - json_fixed_key

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-fetch-format-reusable.yml
    with:
      short_model: grok-4-fast
      full_model: x-ai/grok-4-fast
      format: json_fixed_key
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}