name: Eval Valid YAML Fetch URL - glm-4-32b - yaml

on:
  workflow_run:
    workflows: ["Eval Execution Fetch URL - glm-4-32b - yaml"]
    types:
      - completed

jobs:
  evaluate:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/eval-valid-yaml-format-reusable.yml
    with:
      model: glm-4-32b
      run_id: ${{ github.event.workflow_run.id }}
      format: yaml
