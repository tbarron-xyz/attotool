name: Eval Execution Fetch URL - openai/gpt-5-nano - yaml

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-fetch-format-reusable.yml
    with:
      short_model: openai-gpt-5-nano
      full_model: openai/gpt-5-nano
      format: yaml
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}