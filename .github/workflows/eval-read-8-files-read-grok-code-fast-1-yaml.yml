name: Eval Read 8 Files Read - grok-code-fast-1 - yaml

on:
  workflow_run:
    workflows: ["Eval Execution Read - grok-code-fast-1 - yaml"]
    types:
      - completed

jobs:
  evaluate:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/eval-read-8-files-format-reusable.yml
    with:
      model: grok-code-fast-1
      run_id: ${{ github.event.workflow_run.id }}
      format: yaml
