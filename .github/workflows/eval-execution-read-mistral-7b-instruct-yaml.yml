name: Eval Execution Read - mistral-7b-instruct - yaml

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-read-format-reusable.yml
    with:
      short_model: mistral-7b-instruct
      full_model: mistralai/mistral-7b-instruct
      format: yaml
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}