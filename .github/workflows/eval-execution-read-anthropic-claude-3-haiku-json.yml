name: Eval Execution Read - anthropic/claude-3-haiku - json

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-read-format-reusable.yml
    with:
      short_model: claude-3-haiku
      full_model: anthropic/claude-3-haiku
      format: json
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}