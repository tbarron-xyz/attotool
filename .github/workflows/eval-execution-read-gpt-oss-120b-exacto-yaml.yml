name: Eval Execution Read - gpt-oss-120b-exacto - yaml

on:
  workflow_dispatch:

jobs:
  run-eval:
    uses: ./.github/workflows/eval-execution-read-format-reusable.yml
    with:
      short_model: gpt-oss-120b-exacto
      full_model: openai/gpt-oss-120b:exacto
      format: yaml
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}