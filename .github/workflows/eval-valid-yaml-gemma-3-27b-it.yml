name: Eval Valid YAML - gemma-3-27b-it

on:
  workflow_run:
    workflows: ["Eval Execution Fetch URL - gemma-3-27b-it"]
    types:
      - completed

jobs:
  evaluate:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/eval-valid-yaml-reusable.yml
    with:
      model: gemma-3-27b-it
      run_id: ${{ github.event.workflow_run.id }}
