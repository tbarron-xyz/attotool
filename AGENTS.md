The project context is "attotool", a Rust repository that defines a tiny tool-calling agent who speaks YAML
The Rust source files live in ./src/
After making any code changes to Rust files, run `cargo fmt`